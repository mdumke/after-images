{
  "name": "afterimages",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "build": "npm run clean && parcel build src/index.html --no-source-maps --dist-dir docs --public-url ./after-images/",
    "clean": "rimraf docs && mkdir docs",
    "start": "parcel src/index.html --open",
    "check": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.0",
    "parcel": "^2.8.0",
    "typescript": "^4.9.3"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
